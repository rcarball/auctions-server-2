{
  "properties": [
    {
      "name": "currency.api.url",
      "type": "java.lang.String",
      "description": "Base URL of the external currency conversion service (FreeCurrencyAPI)."
    },
    {
      "name": "currency.api.key",
      "type": "java.lang.String",
      "description": "API key for the external currency conversion service. Prefer providing it via the CURRENCY_API_KEY environment variable."
    }
  ]
}
